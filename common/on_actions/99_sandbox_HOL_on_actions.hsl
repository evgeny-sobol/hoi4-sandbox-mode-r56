on_actions:
  on_startup:
    effect:
      if is_historical_focus_on(no):
        $HOL->update_HOL_national_focuses()

  on_weekly_HOL:
    effect:
      if is_sandbox_mode_on():
        update_HOL_national_focuses()
