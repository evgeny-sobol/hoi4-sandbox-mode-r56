on_actions:
  on_startup:
    effect:
      if is_historical_focus_on(no):
        $AFG->update_AFG_national_focuses()

  on_weekly_AFG:
    effect:
      if is_sandbox_mode_on():
        update_AFG_national_focuses()
