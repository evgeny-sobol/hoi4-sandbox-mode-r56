on_actions:
  on_startup:
    effect:
      if is_historical_focus_on(no):
        $COG->update_COG_national_focuses()

  on_weekly_COG:
    effect:
      if is_sandbox_mode_on():
        update_COG_national_focuses()
