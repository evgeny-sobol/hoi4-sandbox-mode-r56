on_actions:
  on_startup:
    effect:
      if is_historical_focus_on(no):
        $POL->update_POL_national_focuses()

  on_weekly_POL:
    effect:
      if is_sandbox_mode_on():
        update_POL_national_focuses()
