on_actions:
  on_startup:
    effect:
      if is_historical_focus_on(no):
        $GER->update_GER_national_focuses()

  on_weekly_GER:
    effect:
      if is_sandbox_mode_on():
        update_GER_national_focuses()
