focus_tree[id = iraq_focus]:

  focus[id = IRQ_decouple_from_the_pound]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()
      +modifier:
        $ai_mic_modifier()
      +modifier:
        $ai_high_tyranny_tilt()

  focus[id = IRQ_construct_the_kut_barrage]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_strengthen_mesopotamian_farming]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_extensive_irrigation_projects]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_rural_electrification]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_found_the_iraqi_state_railways]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_finish_the_berlin_baghdad_railway]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_purchase_foreign_equipment]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_found_southern_steel_plants]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_found_the_state_company_for_iron_and_steel]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_baghdad_colleges_of_engineering_and_education]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_strengthen_the_iraqi_dinar]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_found_company_for_cement]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_expand_the_cotton_industries]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_heavy_industry_investments]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_local_arms_industry]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_invite_foreign_arms_corporations]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_construct_the_dukan_dam]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_forceful_industrialization]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_nationalize_the_anglo_iraqi_oil_company]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()
      +modifier:
        $ai_cooperation_modifier($ROOT)
      +modifier:
        factor(0)
        $ROOT in rivals[]
        is_sandbox_mode_on()
    available:
      $not_rival_of_PREV($ROOT)

  focus[id = IRQ_anglo_iraqi_oil_expansion]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_form_opec]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_demand_greater_stake_in_the_company]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_expand_oil_production]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_export_infrastructure]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_royal_iraqi_army]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_infantry_focus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_scavenging]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_modernization]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_cavalry_integration]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_expand_camel_corps]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_purchase_mechanized_equipment]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_purchase_armored_equipment]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_approach_tribes_for_support]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_invest_in_ordnance_mountain_howitzers]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_training]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_army_research_bonus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_project_babylon_focus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_form_the_independent_mechanized_brigade]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_expand_habbaniya_airbase]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_independent_flight_schools]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_purchase_british_aircraft]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_purchase_alternative_aricraft]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_bomber_training]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_naval_air_support]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_fighter_training]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_air_research_bonus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_special_projects]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_special_forces]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_establish_iraqi_coastal_defense_force]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_invite_foreign_advisors]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_purchase_small_ships]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_create_repair_yards]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_navy_research_bonus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_basra_military_port_focus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_develop_the_kuwaiti_port]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_ensure_sunni_dominance]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = IRQ_rally_behind_the_hashemite_dynasty]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_increase_anglo_iraqi_economic_ties]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_raf_levies_focus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_kurdish_outreach]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_suppress_tribal_revolts]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_assasination_of_sidqi]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_eliminate_the_golden_square]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_embargo_the_axis_powers]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_support_the_allied_war_effort]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_arab_league]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_arab_league_economic_cooperation]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($IRQ)
      +modifier:
        $ai_rivalry_modifier($IRQ)
      +modifier:
        $ai_betrayal_modifier_vs($IRQ)
      +modifier:
        $ai_mic_modifier()
    available:
      $IRQ->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_protector_of_the_gulf]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_renegotiate_treaty]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_cooperation_modifier($ENG)
      +modifier:
        factor(0)
        $ENG in rivals[]
        is_sandbox_mode_on()
    available:
      $not_rival_of_PREV($ENG)

  focus[id = IRQ_allied_training]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_reach_out_to_jordan]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_regional_economic_integration]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_regional_military_integration]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_demand_palestine]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        antagonism = 1 - ($opinion_factor($PAL) + $opinion_factor($ENG)) / 2
        factor(antagonism)
        is_sandbox_mode_on()
      +modifier:
        $ai_rivalry_modifier_max($PAL, $ENG)
      +modifier:
        $ai_betrayal_modifier()
        or:
          $PAL->is_friend_of_PREV()
          $ENG->is_friend_of_PREV()
    available:
      $PAL->can_PREV_get_wargoal_on_THIS()
      $ENG->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_commander_of_the_faithful]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_increase_prime_ministers_powers]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_demand_syria_hashemite]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($SYR)
      +modifier:
        $ai_rivalry_modifier($SYR)
      +modifier:
        $ai_betrayal_modifier_vs($SYR)
    available:
      $SYR->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_hashemite_arab_federation]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_expand_membership]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_restoration_of_hejaz]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($SAU)
      +modifier:
        $ai_rivalry_modifier($SAU)
      +modifier:
        $ai_betrayal_modifier_vs($SAU)
    available:
      $SAU->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_a_new_caliph]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_unite_mashriq]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        antagonism = 1 - ($opinion_factor($PAL) + $opinion_factor($ENG) + $opinion_factor($ISR) + $opinion_factor($LEB) + $opinion_factor($SYR) + $opinion_factor($SAU) + $opinion_factor($OMA) + $opinion_factor($IMO) + $opinion_factor($YEM) + $opinion_factor($FSA) + $opinion_factor($KUW)) / 11
        factor(antagonism)
        is_sandbox_mode_on()
      +modifier:
        rivalry_vs = 0
        $rivalry_vs_into($PAL)
        $rivalry_vs_into($ENG)
        $rivalry_vs_into($ISR)
        $rivalry_vs_into($LEB)
        $rivalry_vs_into($SYR)
        $rivalry_vs_into($SAU)
        $rivalry_vs_into($OMA)
        $rivalry_vs_into($IMO)
        $rivalry_vs_into($YEM)
        $rivalry_vs_into($FSA)
        $rivalry_vs_into($KUW)
        f = 1 + rivalry_vs / 50
        factor(f)
        is_sandbox_mode_on()
      +modifier:
        $ai_betrayal_modifier()
        or:
          $PAL->is_friend_of_PREV()
          $ENG->is_friend_of_PREV()
          $ISR->is_friend_of_PREV()
          $LEB->is_friend_of_PREV()
          $SYR->is_friend_of_PREV()
          $SAU->is_friend_of_PREV()
          $OMA->is_friend_of_PREV()
          $IMO->is_friend_of_PREV()
          $YEM->is_friend_of_PREV()
          $FSA->is_friend_of_PREV()
          $KUW->is_friend_of_PREV()
    available:
      $PAL->can_PREV_get_wargoal_on_THIS()
      $ENG->can_PREV_get_wargoal_on_THIS()
      $ISR->can_PREV_get_wargoal_on_THIS()
      $LEB->can_PREV_get_wargoal_on_THIS()
      $SYR->can_PREV_get_wargoal_on_THIS()
      $SAU->can_PREV_get_wargoal_on_THIS()
      $OMA->can_PREV_get_wargoal_on_THIS()
      $IMO->can_PREV_get_wargoal_on_THIS()
      $YEM->can_PREV_get_wargoal_on_THIS()
      $FSA->can_PREV_get_wargoal_on_THIS()
      $KUW->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_unite_maghreb]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        antagonism = 1 - ($opinion_factor($ENG) + $opinion_factor($EGY) + $opinion_factor($LBA) + $opinion_factor($ITA) + $opinion_factor($FRA) + $opinion_factor($ALG) + $opinion_factor($TUN)) / 7
        factor(antagonism)
        is_sandbox_mode_on()
      +modifier:
        rivalry_vs = 0
        $rivalry_vs_into($ENG)
        $rivalry_vs_into($EGY)
        $rivalry_vs_into($LBA)
        $rivalry_vs_into($ITA)
        $rivalry_vs_into($FRA)
        $rivalry_vs_into($ALG)
        $rivalry_vs_into($TUN)
        f = 1 + rivalry_vs / 50
        factor(f)
        is_sandbox_mode_on()
      +modifier:
        $ai_betrayal_modifier()
        or:
          $ENG->is_friend_of_PREV()
          $EGY->is_friend_of_PREV()
          $LBA->is_friend_of_PREV()
          $ITA->is_friend_of_PREV()
          $FRA->is_friend_of_PREV()
          $ALG->is_friend_of_PREV()
          $TUN->is_friend_of_PREV()
    available:
      $ENG->can_PREV_get_wargoal_on_THIS()
      $EGY->can_PREV_get_wargoal_on_THIS()
      $LBA->can_PREV_get_wargoal_on_THIS()
      $ITA->can_PREV_get_wargoal_on_THIS()
      $FRA->can_PREV_get_wargoal_on_THIS()
      $ALG->can_PREV_get_wargoal_on_THIS()
      $TUN->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_restore_al_andalus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        antagonism = 1 - ($opinion_factor($RIF) + $opinion_factor($MOR) + $opinion_factor($SPR) + $opinion_factor($POR)) / 4
        factor(antagonism)
        is_sandbox_mode_on()
      +modifier:
        rivalry_vs = 0
        $rivalry_vs_into($RIF)
        $rivalry_vs_into($MOR)
        $rivalry_vs_into($SPR)
        $rivalry_vs_into($POR)
        f = 1 + rivalry_vs / 50
        factor(f)
        is_sandbox_mode_on()
      +modifier:
        $ai_betrayal_modifier()
        or:
          $RIF->is_friend_of_PREV()
          $MOR->is_friend_of_PREV()
          $SPR->is_friend_of_PREV()
          $POR->is_friend_of_PREV()
    available:
      $RIF->can_PREV_get_wargoal_on_THIS()
      $MOR->can_PREV_get_wargoal_on_THIS()
      $SPR->can_PREV_get_wargoal_on_THIS()
      $POR->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_party_of_national_brotherhood]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_bakr_sidqi_coup]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_high_tyranny_tilt()

  focus[id = IRQ_increase_italian_business_relations]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_al_muthanna_club]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_attempted_assasination_of_sidqi]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_curtail_arabism]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_strengthen_relations_with_iran]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_strengthen_relations_with_turkey]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_encourage_federalism]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_reapproachment_with_the_tribes]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_anti_western_alliance]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_carve_up_syria]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($SYR)
      +modifier:
        $ai_rivalry_modifier($SYR)
      +modifier:
        $ai_betrayal_modifier_vs($SYR)
    available:
      $SYR->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_status_of_lebanon]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($LEB)
      +modifier:
        $ai_rivalry_modifier($LEB)
      +modifier:
        $ai_betrayal_modifier_vs($LEB)
    available:
      $LEB->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_align_afghanistan_fasc]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_greater_iraq]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_take_advantage_of_british_weakness]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($ENG)
      +modifier:
        $ai_rivalry_modifier($ENG)
      +modifier:
        $ai_betrayal_modifier_vs($ENG)
    available:
      $ENG->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_strengthen_the_golden_square]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_iraqi_al_futuwwa]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_fligerfuhrer_irak]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_request_syria_from_vichy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($SYR)
      +modifier:
        $ai_rivalry_modifier($SYR)
      +modifier:
        $ai_betrayal_modifier_vs($SYR)
    available:
      $SYR->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_rally_the_arab_world]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_arab_free_legion]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_regime_change_in_arabia]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($SAU)
      +modifier:
        $ai_rivalry_modifier($SAU)
      +modifier:
        $ai_betrayal_modifier_vs($SAU)
    available:
      $SAU->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_iran_ultimatum]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PER)
      +modifier:
        $ai_rivalry_modifier($PER)
      +modifier:
        $ai_betrayal_modifier_vs($PER)
    available:
      $PER->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_liberate_levant]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PREV)
      +modifier:
        $ai_rivalry_modifier($PREV)
      +modifier:
        $ai_betrayal_modifier_vs($PREV)
    available:
      $PREV->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_liberate_north_africa]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PREV)
      +modifier:
        $ai_rivalry_modifier($PREV)
      +modifier:
        $ai_betrayal_modifier_vs($PREV)
    available:
      $PREV->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_proclaim_united_arab_republic]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_revoke_anglo_iraq_treaty]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_cooperation_modifier($ROOT)
      +modifier:
        factor(0)
        $ROOT in rivals[]
        is_sandbox_mode_on()
    available:
      $not_rival_of_PREV($ROOT)

  focus[id = IRQ_join_axis]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        f = 1 + mtth:fascism_factor
        factor(f)
        is_sandbox_mode_on()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_seek_axis_investment]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_axis_training]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_invite_oil_mission]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_restore_regent]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_refuge_for_the_grand_mufti]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_promote_kurdish_culture]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()
      +modifier:
        $ai_civil_war_root_modifier()
    +completion_reward:
      $sandbox_log_cw_root(IRQ_promote_kurdish_culture)

  focus[id = IRQ_kurdish_democratic]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_kurdish_socialist]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_kurdish_revolt]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_civil_war_ignition_modifier()
      +modifier:
        factor(0)
        sandbox_civil_war_cap_reached()
      +modifier:
        $ai_cooperation_modifier($ROOT)
      +modifier:
        factor(0)
        $ROOT in rivals[]
        is_sandbox_mode_on()
    available:
      $not_rival_of_PREV($ROOT)
    completion_reward:
      $sandbox_log_cw_ignition(IRQ_kurdish_revolt)

  focus[id = IRQ_kurdish_language]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_kurdish_industry]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_kurdish_oil]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_kurdish_form_the_peshmerga]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_effectivize_the_production_line]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_kurdish_women_in_military]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_kurdish_reject_ankara_treaty]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_further_ussr_support]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_support_from_skies_above]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()
      modifier:
        $ai_mic_modifier()

  focus[id = IRQ_support_from_the_mountains]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_kurdish_attack_turkey]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($TUR)
      +modifier:
        $ai_rivalry_modifier($TUR)
      +modifier:
        $ai_betrayal_modifier_vs($TUR)
    available:
      $TUR->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_kurdish_attack_iran]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PER)
      +modifier:
        $ai_rivalry_modifier($PER)
      +modifier:
        $ai_betrayal_modifier_vs($PER)
    available:
      $PER->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_kurdish_united]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
