focus_tree[id = samoa_focus]:

  focus[id = SAM_aftermath_of_the_goodwill_mission]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = SAM_with_the_crown]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_reorganising_the_mandate]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_malietoa_leadership]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = SAM_request_the_ellice_islands]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_for_a_united_polynesia]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_request_british_polynesia]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_request_french_holdings]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_request_hawaii]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_proclaim_a_polynesian_protectorate]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_new_zealand_commisar]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = SAM_invest_into_civilian_infanstructure]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_develop_apia_port]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_imperial_airforce_bases]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_commonwealth_air_training_program]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_invite_commonwealth_investors]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_the_japanese_threat]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_fortify_our_coast]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_request_weapons]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_war_economy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_sun_and_mercury]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_our_own_way]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_extend_a_hand_to_america]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_our_place_in_the_empire]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = SAM_invite_commonwealth_teachers]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
