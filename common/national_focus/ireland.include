focus_tree[id = ireland_focus]:

  focus[id = IRE_finna_fail]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = IRE_push_the_constituition]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_president_replacement]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_infectious_disease_wage]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_recognize_the_palce_of_the_church]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_abolish_the_upper_house]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_emergency_powers_act]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_temporary_state_guidance]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRE_broad_censorship_programs]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_ira_defense_force_memebrship]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_promote_neutrality]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = IRE_northern_ireland_for_assistance]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()
      modifier:
        $crossroad_modifier(2)

  focus[id = IRE_fine_gael]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = IRE_rally_rural_ireland]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_hold_public_funding_oppertunities]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_radio_broadcasts]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_oduffy_expulsion]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_oust_the_moderates]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_raise_the_irish_brigade]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_rid_alien_influence]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()
      modifier:
        $ai_mic_modifier()

  focus[id = IRE_accept_dominin_status]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_establish_the_coorperate_state]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_mic_modifier()

  focus[id = IRE_agricultural_pasturing]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_mic_modifier()

  focus[id = IRE_protectionism]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_mic_modifier()

  focus[id = IRE_free_trade]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_mic_modifier()

  focus[id = IRE_irelands_oppertunity]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_claim_the_celtic_world]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_british_arms]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRE_treet]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRE_coa]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()
