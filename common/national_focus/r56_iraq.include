focus_tree[id = R56_iraqi_focus]:

  focus[id = IRQ_arab_industrial_revolution]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = IRQ_the_basis_of_the_economy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_modernisation_of_rural_villages]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_iraqi_republic_railways]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_total_eletrification]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_rapid_industrialization]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_finish_the_railyways]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_rebuild_baghdad]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_ghazis_propaganda_station]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_royal_armament_factories]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_expanded_oil_drilling]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_mosul_military_stockpile]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_labor_camps]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_steel_mining]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_establish_the_univeristy_of_baghdad]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_create_the_iraqi_block_cipher]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_license_cipher_machines]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_design_the_osirak_reactor]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_expand_the_univeristy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_reform_the_army]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = IRQ_anti_colonialism_policy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_demand_persian_gulf_colonies]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_demand_french_colonies]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_demand_cyprus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_remove_dependency_on_british_equipment]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_motorized_army]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_study_new_land_doctrines]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_desert_specialization]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_camel_corps]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_special_forces_r56]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_army_modernisation]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_study_foreign_tanks]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_improve_army_logistics]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_cruisers_experiments]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_elte_tank_forces]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_air_base_expansion]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_fighter_modernisation]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_air_innovations]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_heavy_fighter_concept]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_naval_bomber_experiments]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_medium_bomber_focus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_light_bomber_focus]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_air_modernisations_programme]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_rocket_development]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_persian_gulf_supremacy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_study_foreign_built_ships]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_a_cruiser_navy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_import_submarine_technology]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_commerce_attack]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_the_twin_threats]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_coastal_defense]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_strike_force]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_persian_gulf_navy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_naval_equipment]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_unsupported_monarch]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = IRQ_reinforce_the_monarchy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $root_modifier()

  focus[id = IRQ_opportunistic_oil_nationalization]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_british_embassy2]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_on_the_british_side]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_friendship_with_turkey]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_friendship_with_iran]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_the_sadabat_pact]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_build_our_own_diplomacy_instead]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_the_hashemite_diplomacy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_promote_peaceful_religious_coexistence]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_favor_shiite_officers]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_united_officer_corps]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_reaffirm_supremacy_over_the_army]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_invite_western_investors]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_seek_foreign_industrial_support]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_tip_the_oil_balance]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_the_end_of_the_monarchy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_a_new_kind_of_monarch]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_officers_coup]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_high_tyranny_tilt()

  focus[id = IRQ_political_purification]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_fascist_oil_nationalization]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_cult_of_personaility]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_assyrian_revanchism]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_algaylani_youth]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_german_military_mission]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_establish_the_republican_guard]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_proclaim_the_neo_assyrian_empire]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_demand_british_territories]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_conquer_saudi]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PREV)
      +modifier:
        $ai_rivalry_modifier($PREV)
      +modifier:
        $ai_betrayal_modifier_vs($PREV)
    available:
      $PREV->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_kurdistan_crisis]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PREV)
      +modifier:
        $ai_rivalry_modifier($PREV)
      +modifier:
        $ai_betrayal_modifier_vs($PREV)
    available:
      $PREV->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_demand_old_turkish_lands]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PREV)
      +modifier:
        $ai_rivalry_modifier($PREV)
      +modifier:
        $ai_betrayal_modifier_vs($PREV)
    available:
      $PREV->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_assyria_restored]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_royal_road]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_workers_revolution]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_communist_oil_nationalization]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_appease_the_army]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_socialist_scientists]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_workers_culture]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_heavy_industry_plan]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_ministry_of_propaganda]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_militarized_settlements]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_soviet_funding]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_secret_police]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
    available:
      is_liberal_leader(no)

  focus[id = IRQ_join_the_comitern]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_the_parition_of_persia]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PER)
      +modifier:
        $ai_rivalry_modifier($PER)
      +modifier:
        $ai_betrayal_modifier_vs($PER)
    available:
      $PER->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_operation_sublime_porte]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($TUR)
      +modifier:
        $ai_rivalry_modifier($TUR)
      +modifier:
        $ai_betrayal_modifier_vs($TUR)
    available:
      $TUR->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_the_red_dawn]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_form_the_socialist_union_of_the_mashriq]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_invite_arabia]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_invite_yemen]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_invite_oman]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_ally_the_turkish_communists]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()
      modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_ally_the_iranian_communists]:
    +ai_will_do:
      modifier:
        $ai_sandbox_modifier()
      modifier:
        $crossroad_modifier(2)

  focus[id = IRQ_assisted_development]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_Mawtini]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_conquer_persia]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($PER)
      +modifier:
        $ai_rivalry_modifier($PER)
      +modifier:
        $ai_betrayal_modifier_vs($PER)
    available:
      $PER->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_conquer_turkey]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $crossroad_modifier(2)
      +modifier:
        $ai_war_support_modifier()
      +modifier:
        $ai_antagonism_modifier($TUR)
      +modifier:
        $ai_rivalry_modifier($TUR)
      +modifier:
        $ai_betrayal_modifier_vs($TUR)
    available:
      $TUR->can_PREV_get_wargoal_on_THIS()

  focus[id = IRQ_the_secular_movement]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_foriegn_fighters]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_british_embassy]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_sell_oil_to_the_allies]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
      +modifier:
        $ai_mic_modifier()

  focus[id = IRQ_join_the_allies]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_welfare_state]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_volunteer_corps]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_allied_support]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()

  focus[id = IRQ_a_true_republic]:
    ai_will_do:
      +modifier:
        $ai_sandbox_modifier()
