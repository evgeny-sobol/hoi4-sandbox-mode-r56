SIA_war_fervor_cat:
  SIA_war_fervor_coup:
    activation:
      sandbox_civil_war_ai_allowed()
    timeout_effect:
      if:
        limit:
          sandbox_civil_war_ai_allowed()
        $sandbox_log_cw_event(SIA_war_fervor_coup)
